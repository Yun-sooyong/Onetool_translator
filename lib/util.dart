import 'package:flutter/material.dart';

// EdgeInsets
var titleSymeEdge = const EdgeInsets.symmetric(horizontal: 15);
var bodyEdge = const EdgeInsets.symmetric(vertical: 20, horizontal: 20);
var boxEdge = const EdgeInsets.symmetric(vertical: 8);

Color backgroundColor = const Color(0xffF7F7F9);
Color primaryColor = const Color(0xffD2302C);
Color textFieldTextColor = const Color(0xff580b1c);

// * Radius //
BorderRadius boxRadius = BorderRadius.circular(15);

//* Padding, Margin //
// * Padding
var mainPadding = const EdgeInsets.symmetric(vertical: 10, horizontal: 15);
var boxPadding = const EdgeInsets.all(20);

// * Margin
var boxMargin = const EdgeInsets.only(top: 10);

// * Colors //
Color bColor = const Color(0xffF7F7F9);
Color mColor = const Color(0xff5E7EAF);
Color fColor = const Color(0xffDAf1DE);

Color unSelectedColor = Colors.grey.shade400;
Color selectedColor = mColor;
